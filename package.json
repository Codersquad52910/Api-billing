{
  "name": "apibill-ecosystem",
  "version": "1.0.0",
  "description": "Premium API Billing & Management Ecosystem",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../admin-frontend && npm install",
    "start": "concurrently --kill-others-on-fail \"npm run start-backend\" \"npm run start-frontend\" \"npm run start-admin\"",
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd frontend && npm run dev",
    "start-admin": "cd admin-frontend && npm run dev",
    "build-all": "cd frontend && npm run build && cd ../admin-frontend && npm run build",
    "test": "node backend/tests/health.test.js",
    "test:ci": "bash scripts/test.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
